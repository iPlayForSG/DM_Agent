# 长时间等待与主持过程展示排查

Status: active
Updated: 2026-09-05

## Goal

定位工具与校验后超过五分钟没有回复的原因，修复已确认的传输或等待状态问题，并使主持过程展示当前真实进展。

## Scope / Non-goals

- 先做只读运行状态、子进程和 checkpoint 调度元数据检查；不重放玩家行动、不改玩家存档、不读取完整会话或凭据。
- 公开过程说明与剧情文本可以展示；不输出私有推理，也不以模拟打字代替上游增量。
- 后端处于活动回合时，不通过修改 Python 文件触发热重载打断事务。

## Progress

- [x] 核对根指南、相关记忆、既有传输实现与现场阶段信息。
- [x] 用户确认等待超过五分钟；健康检查正常，游戏 Codex 配置单次超时为 300 秒。
- [x] 定位当前模型调用与传输等待状态：全队败北后仍循环敌方回合，修复预算被反复续期。
- [x] 在独立工作副本中用合成场景复现并修复战斗收尾、预算、传输隔离与等待展示。
- [x] 完成真实传输与合成浏览器验证；前端构建和回归通过，相关长期文档已在副本校准。
- [x] 中断后重新取得最终后端回归结果，并核对文件哈希后同步运行目录。
- [ ] 完成 Git 交付与临时工作副本清理。

## Decisions

- 2026-09-05：不重复提交原行动；先确认原请求是否结束及是否提交。
- 2026-09-05：确认失控循环后仅终止 API 所属 app-server 子进程树；原请求已进入失败终态，暂存工具结果清空，没有重放玩家行动。
- 2026-09-05：全员败北时要求显式 end_encounter 收尾，不由 validator 隐藏改写战斗状态，不凭空决定死亡或被俘。仍有可存续友方时不适用此收尾条件。

## Discoveries

- 旧工具调用传输要求 content 为空；现允许简短公开处理说明，前端展示当前模型调用次数和等待时间，不再堆叠已完成节点。
- Codex 空 MCP 表与 enabled-only 覆盖无法隔离所有配置来源；现只读取名称/协议类型，覆盖为相同协议的禁用条目，另关闭 code_mode_host。未复制命令、URL、环境或凭据。
- 全队已败北时 advance_turn 会再次选中敌人；旧 validator 没有收尾分支，且每轮把工具上限续到当前轮数 + 1，导致持续模型调用。
- 官方文档 MCP 和网页连接当前均失败，协议核对暂以已安装 CLI 生成的 schema 为依据。

## Validation

- [x] 合成回归覆盖敌方先攻击倒最后一名玩家后 3 次模型调用正常结束遭遇、修复预算硬上限、超时后回滚成功工具写入。
- [x] 前端 build 成功；lint 0 errors、2 条既有 warning；12 项 Node 回归通过，含流无 EOF 仍完成与无数据超时。
- [x] 真实 Codex 合成工具调用耗时 10.89s，10 个公开文本片段、1 个应用工具调用；所观察子进程只有 CLI 运行本体，没有个人 MCP 或代码执行助手。
- [x] Playwright + 临时合成 API 场景：公开处理说明、当前第 2 次模型响应、等待秒数、“主持处理中”以及最终回复均显示正常，截图已目视检查。
- [x] 最终完整后端 293 项：292 通过、1 项可选 CLI 测试跳过；真实 CLI 已另行验证。前端最终 build/lint/12 项测试通过。
- [x] 运行目录与已验证副本的修改文件哈希一致；服务 health=ok、前端 HTTP 200。差异空白及 Markdown 链接检查通过。

## Remaining work

- Git 提交、推送和合并；清理临时工作副本。验收进程已结束。

## Resume instructions

1. 先确认后端是否仍在运行玩家回合，避免热重载中断它。
2. 只读取诊断元数据，不读取或保存完整 transcript。
3. 当前 refactor/frontend-design 与 main 的既有改动已交付；本任务起始只有 output 截图未跟踪。
